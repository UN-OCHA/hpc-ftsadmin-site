import { Language } from '../iface';
import strings from './fr.json';

const LANGUAGE: Language = {
  meta: {
    name: 'Français',
    direction: 'ltr',
  },
  strings,
};

export default LANGUAGE;
